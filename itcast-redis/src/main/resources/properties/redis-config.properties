# 单机版 redis连接信息
redis.hostName=192.168.12.168
redis.port=6379
# 控制一个 pool最多有多少个状态为 idle( 空闲的 ) 的 jedis实例
redis.maxIdle=300
#pool最大 jedis实例数
redis.maxTotal=600
# 在 borrow一个 jedis实例时，是否提前进行 validate操作；如果为 true ，则得到的 jedis实例均是可用的
redis.testOnBorrow=true
